networks:
  mynetwork: {}
volumes:
  mysql:
services:
    mysql:
       image: mysql:1.0.0
       networks:
         - mynetwork
       volumes:
            - mysql:/var/lib/mysql

    backend:
     image: backend:1.0.0
     networks:
       - mynetwork
     container_name: backend
     depends_on:
       - mysql

    frontend:
      image: frontend:1.0.0
      networks:
        - mynetwork
      container_name: frontend
      ports:
        - "80:80"
      depends_on:
        - backend